{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-restricted-imports": [
      "warn",
      {
        "patterns": [
          {
            "group": ["@/features/*/**"],
            "message": "Import from feature public API (@/features/<name>) instead of deep paths."
          }
        ]
      }
    ]
  },
  "overrides": [
    {
      "files": ["src/features/**/*"],
      "rules": {
        "no-restricted-imports": [
          "warn",
          {
            "patterns": [
              {
                "group": ["@/features/*/**"],
                "message": "Import from feature public API (@/features/<name>) instead of deep paths."
              }
            ]
          }
        ]
      }
    }
  ]
}
